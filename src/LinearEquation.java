public class LinearEquation {

    //Make sure to scan if same 2 x values correspond to different y's **Fails vertical line text


    //Methods: equation(), slope() , yInt(), distance(), lineInfo() , coordinateForX(double)


}
